# 2026-05-07 libcamera v0.7.1 pipeline handler / sensor configuration rewrite diff

## Article

libcamera v0.7.1: 파이프라인 핸들러 및 센서 구성 업데이트

## Original summary

The archived article connected upstream libcamera pipeline handler and sensor configuration updates directly to camera3_device_ops_t, configure_streams, process_capture_request, stream combinations, and Camera ITS retesting.

## Rewritten summary

The article now treats the change as an upstream driver/image-pipeline update. Android HAL stream configuration or sensor-mode follow-up is only recommended when the downstream vendor stack consumes that libcamera path and regression evidence exists.

## Removed overclaim

- Direct Android Camera HAL API/runtime framing from the upstream libcamera source.
- Blanket CTS/VTS/Camera ITS retesting without downstream integration evidence.
- Stream and buffer log collection as an immediate HAL action for every product.

## Source-backed boundary

The source supports a libcamera release-level pipeline/sensor update. It does not establish Android HAL configure_streams or process_capture_request behavior changes.

## Validation intent

Preserve direct camera relevance while separating upstream driver evidence from downstream HAL runtime validation.
