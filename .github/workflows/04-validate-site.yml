name: 04 - Validate Site and Images

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-site:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Run unit and regression tests
        run: npm run test

      - name: Validate structural publication artifacts
        run: |
          npm run check:encoding
          npm run validate:policy
          npm run check:policy-docs
          npm run validate:config
          npm run validate:site
          npm run validate:images
          npm run validate:localization

      - name: Annotate publication quality and fact-check status
        if: always()
        continue-on-error: true
        run: node scripts/annotate-publication-quality.js --latest
